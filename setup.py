from setuptools import setup, find_packages

setup(
    name='pytorch3d-lite',
    version='2.1.0',
    description='lite version of pytorch3d with just needed functions to run disco diffusion v5 and above',
    packages=find_packages(),
    install_requires=[],
)
